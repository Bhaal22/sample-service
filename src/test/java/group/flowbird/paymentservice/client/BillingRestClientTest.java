package group.flowbird.paymentservice.client;

import group.flowbird.paymentservice.client.ClientDTO.InvoiceDTO;
import org.junit.After;
import org.junit.Before;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.MockitoAnnotations;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

import static org.junit.Assert.*;

//@RunWith(SpringRunner.class)
//@SpringBootTest
public class BillingRestClientTest {
/*
    public static final Long INVOICE_ID = 6354551L;

    @InjectMocks
    BillingRestClient billingRestClient;

    @Mock
    RestClient restClient;

    InvoiceDTO invoiceDTO;


    @Before
    public void setUp() throws Exception {
        MockitoAnnotations.initMocks(this);
    }

    @After
    public void tearDown() throws Exception {
    }

    @Test
    public void getInvoice() {
        //invoiceDTO = billingRestClient.getInvoice(INVOICE_ID);
    }

    @Test
    public void hasOutStandingInvoice() {
        //Boolean invoiceStatus = billingRestClient.hasOutStandingInvoice(invoiceDTO.getCustomerId());
    }

    @Test
    public void updateInvoice() {
        invoiceDTO.setAmount(1000);
        billingRestClient.updateInvoice(invoiceDTO);

        InvoiceDTO newInvoiceDTO = billingRestClient.getInvoice(invoiceDTO.getId());
    }
    */
}